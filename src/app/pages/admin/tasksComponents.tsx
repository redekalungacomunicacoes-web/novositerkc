import { Loader2, Paperclip, Pencil, Trash2, UploadCloud, X, Search, GripVertical } from "lucide-react";
import type { FormEvent, ReactNode } from "react";
import { useMemo, useState } from "react";
import { Avatar, AvatarFallback, AvatarImage } from "@/app/components/ui/avatar";
import { cn } from "@/app/components/ui/utils";
import { HoverCard, HoverCardContent, HoverCardTrigger } from "@/app/components/ui/hover-card";
import { priorityBadgeClass, statusBadgeClass, taskBrandTheme } from "./tasksTheme";
import type { Task, TaskFormValues, TeamProfile } from "./tasksTypes";

const priorityLabel: Record<Task["prioridade"], string> = { urgente: "Urgente", alta: "Alta", media: "Média", baixa: "Baixa", concluida: "Concluída" };
const priorityDotClass: Record<Task["prioridade"], string> = { urgente: "bg-rose-500", alta: "bg-orange-500", media: "bg-blue-500", baixa: "bg-emerald-500", concluida: "bg-slate-400" };

export const PriorityBadge = ({ prioridade }: { prioridade: Task["prioridade"] }) => <span className={cn("inline-flex rounded-full border px-2.5 py-1 text-xs font-semibold", priorityBadgeClass[prioridade])}>{priorityLabel[prioridade]}</span>;

function TaskFormContainer({ children }: { children: ReactNode }) { return <div className="mx-auto w-full max-w-[980px] rounded-2xl border border-slate-200 bg-white p-6 shadow-sm md:p-7">{children}</div>; }
function TabsNavigation({ tab, onChange }: { tab: "detalhes" | "arquivos"; onChange: (tab: "detalhes" | "arquivos") => void }) { return <div className="flex flex-wrap gap-2 border-b border-slate-200 pb-3">{(["detalhes", "arquivos"] as const).map((item) => <button key={item} type="button" onClick={() => onChange(item)} className={cn("rounded-full px-4 py-2 text-sm font-medium capitalize transition-all duration-200", tab === item ? "bg-slate-900 text-white shadow" : "bg-slate-100 text-slate-600 hover:bg-slate-200")}>{item}</button>)}</div>; }
function InputField({ label, children }: { label: string; children: ReactNode }) { return <label className="space-y-2"><span className="text-sm font-medium text-slate-700">{label}</span>{children}</label>; }
function TextAreaField({ label, ...props }: { label: string } & React.TextareaHTMLAttributes<HTMLTextAreaElement>) { return <InputField label={label}><textarea {...props} className={cn("min-h-28 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-900 outline-none transition-all duration-200 focus:border-slate-400 focus:ring-2 focus:ring-slate-200", props.className)} /></InputField>; }
function DatePicker(props: React.InputHTMLAttributes<HTMLInputElement>) { return <input type="date" {...props} className={cn("h-11 w-full rounded-lg border border-slate-300 px-3 text-sm outline-none transition-all duration-200 focus:border-slate-400 focus:ring-2 focus:ring-slate-200", props.className)} />; }

function ResponsibleSelect({
  users,
  value,
  onChange,
  required = false,
  disabled = false,
}: {
  users: TeamProfile[];
  value: string;
  onChange: (id: string) => void;
  required?: boolean;
  disabled?: boolean;
}) {
  const [search, setSearch] = useState("");
  const filtered = users.filter((u) => `${u.nome ?? ""} ${u.email ?? ""}`.toLowerCase().includes(search.toLowerCase()));
  return <div className={cn("space-y-2 rounded-xl border p-3 transition-colors", disabled ? "border-slate-200 bg-slate-50" : "border-slate-200 bg-white hover:border-slate-300")}><div className="relative"><Search className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-slate-400" /><input placeholder="Buscar responsável" value={search} onChange={(e) => setSearch(e.target.value)} disabled={disabled} className="h-10 w-full rounded-lg border border-slate-300 pl-9 pr-3 text-sm outline-none transition-all duration-200 focus:border-slate-400 focus:ring-2 focus:ring-slate-200 disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-400" /></div><select required={required} disabled={disabled} value={value} onChange={(e) => onChange(e.target.value)} className="h-11 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm outline-none transition-all duration-200 focus:border-slate-400 focus:ring-2 focus:ring-slate-200 disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-400"><option value="">{disabled ? "Selecione o direcionamento primeiro" : "Selecione o responsável"}</option>{filtered.map((user) => <option key={user.id} value={user.id}>{user.nome || "Usuário"}{user.email ? ` (${user.email})` : ""}</option>)}</select></div>;
}

function UserSelector({ users, value, onChange }: { users: TeamProfile[]; value: string[]; onChange: (ids: string[]) => void }) { return <div className="space-y-2">{users.map((u) => { const checked = value.includes(u.id); return <label key={u.id} className={cn("flex cursor-pointer items-center gap-3 rounded-xl border p-3 transition duration-200", checked ? "border-slate-900 bg-slate-100" : "border-slate-200 bg-white hover:bg-slate-50")}><input type="checkbox" checked={checked} onChange={(e) => onChange(e.target.checked ? [...new Set([...value, u.id])] : value.filter((id) => id !== u.id))} className="h-4 w-4 rounded border-slate-300" /><Avatar className="h-8 w-8"><AvatarImage alt={u.nome ?? u.email ?? "Usuário"} /><AvatarFallback>{(u.nome ?? u.email ?? "U").slice(0, 1).toUpperCase()}</AvatarFallback></Avatar><span className="text-sm text-slate-800">{u.nome || u.email || "Usuário"}</span></label>; })}</div>; }
function FileUploadArea({ files, onFilesChange }: { files: File[]; onFilesChange: (f: File[]) => void }) { return <div className="space-y-3"><label className="flex min-h-36 cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed border-slate-300 bg-slate-50 p-5 text-center transition duration-200 hover:border-slate-400 hover:bg-slate-100"><UploadCloud className="mb-2 h-7 w-7 text-slate-500" /><span className="text-sm font-medium text-slate-700">Arraste arquivos ou clique para enviar</span><span className="text-xs text-slate-500">Múltiplos anexos permitidos</span><input type="file" multiple className="hidden" onChange={(e) => onFilesChange(Array.from(e.target.files ?? []))} /></label>{files.length > 0 ? <ul className="space-y-2">{files.map((file) => <li key={`${file.name}-${file.size}`} className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700"><Paperclip className="h-4 w-4" /> {file.name}</li>)}</ul> : null}</div>; }

export function TasksCalendarGrid({ calendarDays, monthStartOffset, month, selectedDateKey, tasksByDate, onSelectDate, onPrevMonth, onNextMonth, isLoading }: { calendarDays: Date[]; monthStartOffset: number; month: Date; selectedDateKey: string; tasksByDate: Map<string, Task[]>; onSelectDate: (d: Date) => void; onPrevMonth: () => void; onNextMonth: () => void; isLoading: boolean }) {
  const weekDays = ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sáb"];
  return <section className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm" style={{ ["--brand-primary" as string]: taskBrandTheme.brandPrimary }}><div className="mb-4 flex items-center justify-between"><div className="flex items-center gap-2"><button className="rounded-lg border px-3 py-2 text-sm" onClick={() => onSelectDate(new Date())}>Hoje</button><button onClick={onPrevMonth} className="rounded-lg border px-3 py-2">◀</button><button onClick={onNextMonth} className="rounded-lg border px-3 py-2">▶</button></div><p className="text-lg font-semibold capitalize">{month.toLocaleDateString("pt-BR", { month: "long", year: "numeric" })}</p><div className="text-sm text-slate-500">Mês / Semana / Dia</div></div><div className="grid grid-cols-7 gap-2">{weekDays.map((w) => <div key={w} className="px-2 py-1 text-xs font-semibold text-slate-500">{w}</div>)}{Array.from({ length: monthStartOffset }).map((_, i) => <div key={i} />)}{calendarDays.map((day) => { const key = `${day.getFullYear()}-${`${day.getMonth() + 1}`.padStart(2, "0")}-${`${day.getDate()}`.padStart(2, "0")}`; const dayTasks = tasksByDate.get(key) ?? []; const visible = dayTasks.slice(0, 2); const remaining = dayTasks.length - visible.length; return <button key={key} onClick={() => onSelectDate(day)} className={cn("min-h-28 rounded-xl border border-slate-200 p-2 text-left transition duration-200 hover:bg-slate-50", key === selectedDateKey && "border-emerald-500 bg-emerald-50/60")}><div className="mb-1 text-sm font-semibold">{day.getDate()}</div><div className="space-y-1">{visible.map((task) => <HoverCard key={task.id}><HoverCardTrigger asChild><div className="flex items-center gap-1.5 rounded bg-slate-100 px-2 py-1 text-xs"><span className={cn("h-2 w-2 rounded-full", priorityDotClass[task.prioridade])} /><span className="truncate">{task.titulo}</span></div></HoverCardTrigger><HoverCardContent className="w-72"><p className="font-semibold">{task.titulo}</p><p className="text-xs text-slate-500">{task.data_tarefa} {task.hora_inicio ?? ""}</p></HoverCardContent></HoverCard>)}</div>{remaining > 0 ? <p className="mt-1 text-xs text-slate-500">+{remaining} mais</p> : null}</button>; })}</div>{isLoading ? <div className="mt-3 animate-pulse rounded-lg bg-slate-100 p-3 text-sm text-slate-400">Carregando calendário…</div> : null}</section>;
}

export function TasksDrawer(props: any) { /* keep loose for speed */
  const [tab, setTab] = useState<"detalhes" | "arquivos">("detalhes");
  const finalDateInvalid = useMemo(() => props.form.data_final < props.form.data_inicial, [props.form.data_final, props.form.data_inicial]);
  const direcionamentoUsers = props.users.filter((u: TeamProfile) => props.form.direcionamento.includes(u.id));
  if (!props.isOpen) return null;
  return <div className="fixed inset-0 z-50 overflow-y-auto bg-black/45 p-4 md:p-8"><div className="ml-auto w-full max-w-5xl"><TaskFormContainer><div className="mb-4 flex items-start justify-between"><div><h2 className="text-2xl font-semibold text-slate-900">Nova tarefa</h2><p className="text-sm text-slate-500">{props.selectedDate.toLocaleDateString("pt-BR")}</p></div><button onClick={props.onClose} className="rounded-md p-2 text-slate-500 hover:bg-slate-100"><X /></button></div><div className="mb-6 space-y-3">{props.tasksOfDay.map((task: Task) => <article key={task.id} draggable className="rounded-xl border border-slate-200 bg-slate-50 p-3"><div className="mb-1 flex items-center gap-2"><GripVertical className="h-4 w-4 text-slate-400" /><PriorityBadge prioridade={task.prioridade} /><span className={cn("rounded-full border px-2 py-0.5 text-xs", statusBadgeClass[task.status])}>{task.status}</span><button onClick={() => props.onEditTask(task)} className="ml-auto rounded p-1 hover:bg-slate-200"><Pencil className="h-4 w-4" /></button><button onClick={() => props.onDeleteTask(task.id)} className="rounded p-1 text-rose-600 hover:bg-rose-100"><Trash2 className="h-4 w-4" /></button></div><p className="text-sm font-semibold text-slate-900">{task.titulo}</p></article>)}</div><form onSubmit={props.onSubmit} className="space-y-5"><TabsNavigation tab={tab} onChange={setTab} />{tab==="detalhes"&&<div className="grid grid-cols-1 gap-5 md:grid-cols-2"><div className="md:col-span-2"><InputField label="Título"><input required value={props.form.titulo} onChange={(e)=>props.onFormChange({titulo:e.target.value})} className="h-11 w-full rounded-lg border border-slate-300 px-3 text-sm"/></InputField></div><div className="md:col-span-2"><TextAreaField label="Descrição" value={props.form.descricao} onChange={(e)=>props.onFormChange({descricao:e.target.value})}/></div><InputField label="Data inicial"><DatePicker value={props.form.data_inicial} onChange={(e)=>props.onFormChange({data_inicial:e.target.value})}/></InputField><InputField label="Data final"><DatePicker value={props.form.data_final} onChange={(e)=>props.onFormChange({data_final:e.target.value})}/></InputField><div className="md:col-span-2"><InputField label="Direcionamento"><UserSelector users={props.users} value={props.form.direcionamento} onChange={(ids)=>props.onFormChange({direcionamento:ids})} /></InputField></div><div className="md:col-span-2"><InputField label="Responsável da tarefa"><ResponsibleSelect required users={direcionamentoUsers} disabled={props.form.direcionamento.length === 0} value={props.form.assigned_to} onChange={(id)=>props.onFormChange({assigned_to:id})} /></InputField></div></div>}{tab==="arquivos"&&<div className="space-y-5"><FileUploadArea files={props.files} onFilesChange={props.onFilesChange} /><TextAreaField label="Observações (aceita links)" value={props.form.observacoes} onChange={(e)=>props.onFormChange({observacoes:e.target.value})} className="min-h-40" /></div>}{finalDateInvalid?<p className="rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-sm text-amber-800">A data final não pode ser menor que a data inicial.</p>:null}{props.saveError?<p className="rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-sm text-rose-700">{props.saveError}</p>:null}<div className="flex justify-end"><button disabled={props.isSaving || finalDateInvalid} className="inline-flex items-center gap-2 rounded-lg bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">{props.isSaving ? <><Loader2 className="h-4 w-4 animate-spin" /> Salvando...</> : "Salvar tarefa"}</button></div></form></TaskFormContainer></div></div>;
}
